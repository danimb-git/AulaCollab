{
  "name": "aulacollab",
  "private": true,

  "scripts": {
    "studio:azure": "dotenv -e .env.azure -- npx prisma studio",
    "studio:local": "dotenv -e .env.local -- npx prisma studio",
    "pull:azure": "dotenv -e .env.azure -- npx prisma db pull",
    "generate": "npx prisma generate"
  },

  "dependencies": {
    "@prisma/client": "^6.19.2",
    "dotenv-cli": "^11.0.0"
  },

  "devDependencies": {
    "prisma": "^6.19.2",
    "@types/node": "^25.0.9"
  }
}
